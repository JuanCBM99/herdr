# Initialize herdr project folders and example data

This function creates the 'user_data', 'Examples', and 'output' folders
in your current working directory and populates them with the default
files from the package.

## Usage

``` r
herdr_init()
```
